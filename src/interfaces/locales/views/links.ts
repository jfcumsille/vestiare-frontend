export interface LinksViewLocale {
  filters: {
    searchBarPlaceholder: string,
    active: {
      label: string,
      options: {
        all: string,
        active: string,
        inactive: string
      },
    },
    password: {
      label: string,
      options: {
        all: string,
        valid: string,
        invalid: string
      },
    },
  },
}
